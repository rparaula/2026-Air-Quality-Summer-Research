# tests/fixtures

This folder is reserved for shared test fixtures (sample CSVs, JSON blobs, shapefiles, or reusable helper assets) that are consumed by tests in sibling folders.

## Current status

- No fixture scripts are currently present.
- No methods are tested directly from this folder at the moment.

When fixture files are added, document which test scripts consume them and which production methods they support.
